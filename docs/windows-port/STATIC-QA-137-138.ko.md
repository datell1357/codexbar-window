# 공급자 설정 저장·편집창 정적 리뷰 — 수정 중

137: 저장 후 전체 활성 공급자 설정 캐시를 재구성하지 않고 편집한 공급자만 갱신해 다른 공급자에 잘못된 임계값을 적용할 수 있다. 수정 진행 중.

138: 창 제목 공급자명 보간 누락 및 잘못된 입력으로 모드 전환이 거부된 뒤 라디오 표시가 draft와 불일치한다. 입력값을 보존하면서 선택 표시만 되돌리도록 수정 중.

137의 사용량 표시 generation 차단 제안은 채택하지 않는다. 이번 generation은 경고 설정 저장만 추적하며 사용량 조회 입력은 변경하지 않는다. 이전 경고 정책의 상태 변경과 알림만 차단하고 조회 결과 표시는 유지하는 것이 승인된 범위다.

WM_QUIT 재게시는 중첩 메시지 루프에서 소비한 종료 요청을 외부 루프에 전달하기 위한 기존 동작이며 유지한다.

모든 내용은 정적 소스 대조에 한정한다. 재검토 전 커밋 승인/기능 완료로 집계하지 않는다. 140에서 트레이 비동기 연결을 구현 중이다.

## 재검토

137 캐시·경고 세대 정적 검토 승인. load/save 단계 오류 접두사를 추가하고 중복 catch를 정리했다. 138 편집창 재검토 승인. 140 호스트 연결은 별도 수정·리뷰 중이며 통합 완료로 간주하지 않는다.
