# Windows 학습 이력 연결 정적 검토 172–175

범위 제한 APPROVE. 성공한 Codex 주간 표본은 예측 알림 토글과 독립적으로 기록한다. canonical provider-account/email-hash 키, 단일 데이터셋 캐시, 갱신 시작 세대 전달, await 전후 활성 상태·설정 검사를 대조했다. 토큰 UUID를 history 키에 넣어야 한다는 지적은 원본 canonical 소유권과 재대조 후 철회했다.

학습 결과가 있을 때 기대 사용량 하한을 적용하고, 결과가 없을 때만 10080분 기본값의 선형 계산으로 돌아간다. 명시적 근무일은 학습 계산보다 우선한다. 신뢰도·잔여량 조건을 두 경로에 동일하게 적용한다.

리뷰174에서 발견한 설정 변경 후 갱신 지연을 수정했다. 진행 중 갱신이 있으면 한 번의 후속 갱신으로 합치고, 종료 시 큐를 비운다. 리뷰175에서 최종 변경을 확인했다.

미완료: 레거시 소유권 연속성 병합, 대시보드 역채우기 권한, 전체 이식. 이번 단계는 canonical 기록만 읽으며 legacy 인자는 nil, 다중 계정 veto는 true로 유지한다. 기록 파일은 설정 해제 시 삭제하지 않는다. 빌드·테스트·컴파일·실제 기록·조회는 실행하지 않았다.
